// This File is an Helper function
// For file upload to IPFS

const handleSubmit = async (e, fileUrl) => {
  // Call a function that talk the smart Connect (ABI)
  console.log(fileUrl);

  e.preventDefault();
};

export default handleSubmit;
